{% extends 'base.html.twig' %}

{%  block body %}

    <div id="page-wrapper">
        <div class="container-fluid">
            <div class="row bg-title">
                <div class="col-lg-3 col-md-4 col-sm-4 col-xs-12">
                    <h4 class="page-title">Etat</h4> </div>
                <div class="col-lg-9 col-sm-8 col-md-8 col-xs-12">
                    <ol class="breadcrumb">
                        <a href="{{ path("new-etat") }}">Nouvel Etat</a>
                    </ol>
                </div>
                <!-- /.col-lg-12 -->
            </div>
            <div class="row">
                <div class="col-sm-12">
                    <div class="white-box">
                        <h3 class="box-title"></h3>
                        <div class="table-responsive">
                            <table class="table">
                                <thead>
                                <tr>
                                    <td>Etat</td>
                                    <td>Action</td>
                                </tr>
                                </thead>

                                {% for etat in etats %}
                                <tr>
                                    <td>{{ etat.nameEtat }}</td>
                                    <td>
                                        <a href={{ path('edit-etat', {id:etat.id}) }}>Modifier</a>
                                        <a href={{ path('delete-etat', {id:etat.id}) }}>Supprimer</a>
                                        {#<a href="{{ path('details-client', {id:client.id}) }}"> Détails</a>#}
                                    </td>
                                <tr>
                                    {% endfor %}

                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}